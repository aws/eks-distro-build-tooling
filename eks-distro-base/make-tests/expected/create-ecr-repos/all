
# This file was generated by running `make run-make-tests` and `make update-make-tests-expected`. DO NOT EDIT

cmd=( ecr ); \
	if [[ "localhost:5000" =~ ^public\.ecr\.aws/ ]]; then \
		cmd=( ecr-public --region us-east-1 ); \
	fi; \
	repo=eks-distro-base; \
	if ! aws ${cmd[*]} describe-repositories --repository-name "$repo" > /dev/null 2>&1; then \
		aws ${cmd[*]} create-repository --repository-name "$repo"; \
	fi;
cmd=( ecr ); \
	if [[ "localhost:5000" =~ ^public\.ecr\.aws/ ]]; then \
		cmd=( ecr-public --region us-east-1 ); \
	fi; \
	repo=eks-distro-minimal-base; \
	if ! aws ${cmd[*]} describe-repositories --repository-name "$repo" > /dev/null 2>&1; then \
		aws ${cmd[*]} create-repository --repository-name "$repo"; \
	fi;
cmd=( ecr ); \
	if [[ "localhost:5000" =~ ^public\.ecr\.aws/ ]]; then \
		cmd=( ecr-public --region us-east-1 ); \
	fi; \
	repo=eks-distro-minimal-base-builder; \
	if ! aws ${cmd[*]} describe-repositories --repository-name "$repo" > /dev/null 2>&1; then \
		aws ${cmd[*]} create-repository --repository-name "$repo"; \
	fi;
cmd=( ecr ); \
	if [[ "localhost:5000" =~ ^public\.ecr\.aws/ ]]; then \
		cmd=( ecr-public --region us-east-1 ); \
	fi; \
	repo=eks-distro-minimal-base-nonroot; \
	if ! aws ${cmd[*]} describe-repositories --repository-name "$repo" > /dev/null 2>&1; then \
		aws ${cmd[*]} create-repository --repository-name "$repo"; \
	fi;
cmd=( ecr ); \
	if [[ "localhost:5000" =~ ^public\.ecr\.aws/ ]]; then \
		cmd=( ecr-public --region us-east-1 ); \
	fi; \
	repo=eks-distro-minimal-base-nonroot-builder; \
	if ! aws ${cmd[*]} describe-repositories --repository-name "$repo" > /dev/null 2>&1; then \
		aws ${cmd[*]} create-repository --repository-name "$repo"; \
	fi;
cmd=( ecr ); \
	if [[ "localhost:5000" =~ ^public\.ecr\.aws/ ]]; then \
		cmd=( ecr-public --region us-east-1 ); \
	fi; \
	repo=eks-distro-minimal-base-glibc; \
	if ! aws ${cmd[*]} describe-repositories --repository-name "$repo" > /dev/null 2>&1; then \
		aws ${cmd[*]} create-repository --repository-name "$repo"; \
	fi;
cmd=( ecr ); \
	if [[ "localhost:5000" =~ ^public\.ecr\.aws/ ]]; then \
		cmd=( ecr-public --region us-east-1 ); \
	fi; \
	repo=eks-distro-minimal-base-glibc-builder; \
	if ! aws ${cmd[*]} describe-repositories --repository-name "$repo" > /dev/null 2>&1; then \
		aws ${cmd[*]} create-repository --repository-name "$repo"; \
	fi;
cmd=( ecr ); \
	if [[ "localhost:5000" =~ ^public\.ecr\.aws/ ]]; then \
		cmd=( ecr-public --region us-east-1 ); \
	fi; \
	repo=eks-distro-minimal-base-iptables; \
	if ! aws ${cmd[*]} describe-repositories --repository-name "$repo" > /dev/null 2>&1; then \
		aws ${cmd[*]} create-repository --repository-name "$repo"; \
	fi;
cmd=( ecr ); \
	if [[ "localhost:5000" =~ ^public\.ecr\.aws/ ]]; then \
		cmd=( ecr-public --region us-east-1 ); \
	fi; \
	repo=eks-distro-minimal-base-iptables-builder; \
	if ! aws ${cmd[*]} describe-repositories --repository-name "$repo" > /dev/null 2>&1; then \
		aws ${cmd[*]} create-repository --repository-name "$repo"; \
	fi;
cmd=( ecr ); \
	if [[ "localhost:5000" =~ ^public\.ecr\.aws/ ]]; then \
		cmd=( ecr-public --region us-east-1 ); \
	fi; \
	repo=eks-distro-minimal-base-nsenter; \
	if ! aws ${cmd[*]} describe-repositories --repository-name "$repo" > /dev/null 2>&1; then \
		aws ${cmd[*]} create-repository --repository-name "$repo"; \
	fi;
cmd=( ecr ); \
	if [[ "localhost:5000" =~ ^public\.ecr\.aws/ ]]; then \
		cmd=( ecr-public --region us-east-1 ); \
	fi; \
	repo=eks-distro-minimal-base-nsenter-builder; \
	if ! aws ${cmd[*]} describe-repositories --repository-name "$repo" > /dev/null 2>&1; then \
		aws ${cmd[*]} create-repository --repository-name "$repo"; \
	fi;
cmd=( ecr ); \
	if [[ "localhost:5000" =~ ^public\.ecr\.aws/ ]]; then \
		cmd=( ecr-public --region us-east-1 ); \
	fi; \
	repo=eks-distro-minimal-base-docker-client; \
	if ! aws ${cmd[*]} describe-repositories --repository-name "$repo" > /dev/null 2>&1; then \
		aws ${cmd[*]} create-repository --repository-name "$repo"; \
	fi;
cmd=( ecr ); \
	if [[ "localhost:5000" =~ ^public\.ecr\.aws/ ]]; then \
		cmd=( ecr-public --region us-east-1 ); \
	fi; \
	repo=eks-distro-minimal-base-docker-client-builder; \
	if ! aws ${cmd[*]} describe-repositories --repository-name "$repo" > /dev/null 2>&1; then \
		aws ${cmd[*]} create-repository --repository-name "$repo"; \
	fi;
cmd=( ecr ); \
	if [[ "localhost:5000" =~ ^public\.ecr\.aws/ ]]; then \
		cmd=( ecr-public --region us-east-1 ); \
	fi; \
	repo=eks-distro-minimal-base-nginx; \
	if ! aws ${cmd[*]} describe-repositories --repository-name "$repo" > /dev/null 2>&1; then \
		aws ${cmd[*]} create-repository --repository-name "$repo"; \
	fi;
cmd=( ecr ); \
	if [[ "localhost:5000" =~ ^public\.ecr\.aws/ ]]; then \
		cmd=( ecr-public --region us-east-1 ); \
	fi; \
	repo=eks-distro-minimal-base-nginx-builder; \
	if ! aws ${cmd[*]} describe-repositories --repository-name "$repo" > /dev/null 2>&1; then \
		aws ${cmd[*]} create-repository --repository-name "$repo"; \
	fi;
cmd=( ecr ); \
	if [[ "localhost:5000" =~ ^public\.ecr\.aws/ ]]; then \
		cmd=( ecr-public --region us-east-1 ); \
	fi; \
	repo=eks-distro-minimal-base-csi-ebs; \
	if ! aws ${cmd[*]} describe-repositories --repository-name "$repo" > /dev/null 2>&1; then \
		aws ${cmd[*]} create-repository --repository-name "$repo"; \
	fi;
cmd=( ecr ); \
	if [[ "localhost:5000" =~ ^public\.ecr\.aws/ ]]; then \
		cmd=( ecr-public --region us-east-1 ); \
	fi; \
	repo=eks-distro-minimal-base-csi-ebs-builder; \
	if ! aws ${cmd[*]} describe-repositories --repository-name "$repo" > /dev/null 2>&1; then \
		aws ${cmd[*]} create-repository --repository-name "$repo"; \
	fi;
cmd=( ecr ); \
	if [[ "localhost:5000" =~ ^public\.ecr\.aws/ ]]; then \
		cmd=( ecr-public --region us-east-1 ); \
	fi; \
	repo=eks-distro-minimal-base-csi; \
	if ! aws ${cmd[*]} describe-repositories --repository-name "$repo" > /dev/null 2>&1; then \
		aws ${cmd[*]} create-repository --repository-name "$repo"; \
	fi;
cmd=( ecr ); \
	if [[ "localhost:5000" =~ ^public\.ecr\.aws/ ]]; then \
		cmd=( ecr-public --region us-east-1 ); \
	fi; \
	repo=eks-distro-minimal-base-csi-builder; \
	if ! aws ${cmd[*]} describe-repositories --repository-name "$repo" > /dev/null 2>&1; then \
		aws ${cmd[*]} create-repository --repository-name "$repo"; \
	fi;
cmd=( ecr ); \
	if [[ "localhost:5000" =~ ^public\.ecr\.aws/ ]]; then \
		cmd=( ecr-public --region us-east-1 ); \
	fi; \
	repo=eks-distro-minimal-base-kind; \
	if ! aws ${cmd[*]} describe-repositories --repository-name "$repo" > /dev/null 2>&1; then \
		aws ${cmd[*]} create-repository --repository-name "$repo"; \
	fi;
cmd=( ecr ); \
	if [[ "localhost:5000" =~ ^public\.ecr\.aws/ ]]; then \
		cmd=( ecr-public --region us-east-1 ); \
	fi; \
	repo=eks-distro-minimal-base-kind-builder; \
	if ! aws ${cmd[*]} describe-repositories --repository-name "$repo" > /dev/null 2>&1; then \
		aws ${cmd[*]} create-repository --repository-name "$repo"; \
	fi;
cmd=( ecr ); \
	if [[ "localhost:5000" =~ ^public\.ecr\.aws/ ]]; then \
		cmd=( ecr-public --region us-east-1 ); \
	fi; \
	repo=eks-distro-minimal-base-haproxy; \
	if ! aws ${cmd[*]} describe-repositories --repository-name "$repo" > /dev/null 2>&1; then \
		aws ${cmd[*]} create-repository --repository-name "$repo"; \
	fi;
cmd=( ecr ); \
	if [[ "localhost:5000" =~ ^public\.ecr\.aws/ ]]; then \
		cmd=( ecr-public --region us-east-1 ); \
	fi; \
	repo=eks-distro-minimal-base-haproxy-builder; \
	if ! aws ${cmd[*]} describe-repositories --repository-name "$repo" > /dev/null 2>&1; then \
		aws ${cmd[*]} create-repository --repository-name "$repo"; \
	fi;
cmd=( ecr ); \
	if [[ "localhost:5000" =~ ^public\.ecr\.aws/ ]]; then \
		cmd=( ecr-public --region us-east-1 ); \
	fi; \
	repo=eks-distro-minimal-base-git; \
	if ! aws ${cmd[*]} describe-repositories --repository-name "$repo" > /dev/null 2>&1; then \
		aws ${cmd[*]} create-repository --repository-name "$repo"; \
	fi;
cmd=( ecr ); \
	if [[ "localhost:5000" =~ ^public\.ecr\.aws/ ]]; then \
		cmd=( ecr-public --region us-east-1 ); \
	fi; \
	repo=eks-distro-minimal-base-git-builder; \
	if ! aws ${cmd[*]} describe-repositories --repository-name "$repo" > /dev/null 2>&1; then \
		aws ${cmd[*]} create-repository --repository-name "$repo"; \
	fi;
cmd=( ecr ); \
	if [[ "localhost:5000" =~ ^public\.ecr\.aws/ ]]; then \
		cmd=( ecr-public --region us-east-1 ); \
	fi; \
	repo=eks-distro-minimal-base-python; \
	if ! aws ${cmd[*]} describe-repositories --repository-name "$repo" > /dev/null 2>&1; then \
		aws ${cmd[*]} create-repository --repository-name "$repo"; \
	fi;
cmd=( ecr ); \
	if [[ "localhost:5000" =~ ^public\.ecr\.aws/ ]]; then \
		cmd=( ecr-public --region us-east-1 ); \
	fi; \
	repo=eks-distro-minimal-base-python-builder; \
	if ! aws ${cmd[*]} describe-repositories --repository-name "$repo" > /dev/null 2>&1; then \
		aws ${cmd[*]} create-repository --repository-name "$repo"; \
	fi;
cmd=( ecr ); \
	if [[ "localhost:5000" =~ ^public\.ecr\.aws/ ]]; then \
		cmd=( ecr-public --region us-east-1 ); \
	fi; \
	repo=compiler-base; \
	if ! aws ${cmd[*]} describe-repositories --repository-name "$repo" > /dev/null 2>&1; then \
		aws ${cmd[*]} create-repository --repository-name "$repo"; \
	fi;
cmd=( ecr ); \
	if [[ "localhost:5000" =~ ^public\.ecr\.aws/ ]]; then \
		cmd=( ecr-public --region us-east-1 ); \
	fi; \
	repo=compiler-base-builder; \
	if ! aws ${cmd[*]} describe-repositories --repository-name "$repo" > /dev/null 2>&1; then \
		aws ${cmd[*]} create-repository --repository-name "$repo"; \
	fi;
cmd=( ecr ); \
	if [[ "localhost:5000" =~ ^public\.ecr\.aws/ ]]; then \
		cmd=( ecr-public --region us-east-1 ); \
	fi; \
	repo=golang; \
	if ! aws ${cmd[*]} describe-repositories --repository-name "$repo" > /dev/null 2>&1; then \
		aws ${cmd[*]} create-repository --repository-name "$repo"; \
	fi;
cmd=( ecr ); \
	if [[ "localhost:5000" =~ ^public\.ecr\.aws/ ]]; then \
		cmd=( ecr-public --region us-east-1 ); \
	fi; \
	repo=golang-builder; \
	if ! aws ${cmd[*]} describe-repositories --repository-name "$repo" > /dev/null 2>&1; then \
		aws ${cmd[*]} create-repository --repository-name "$repo"; \
	fi;
cmd=( ecr ); \
	if [[ "localhost:5000" =~ ^public\.ecr\.aws/ ]]; then \
		cmd=( ecr-public --region us-east-1 ); \
	fi; \
	repo=eks-distro-minimal-images-base-test; \
	if ! aws ${cmd[*]} describe-repositories --repository-name "$repo" > /dev/null 2>&1; then \
		aws ${cmd[*]} create-repository --repository-name "$repo"; \
	fi;
