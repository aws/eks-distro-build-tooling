
# This file was generated by running `make run-make-tests` and `make update-make-tests-expected`. DO NOT EDIT

tag from file for eks-distro-base: some-previous-tag
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring eks-distro-base tags 'some-previous-tag latest latest.2 latest-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag latest latest.2 latest-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/eks-distro-base:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/eks-distro-base:$tag; done
tag from file for eks-distro-minimal-base: some-previous-tag
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring eks-distro-minimal-base tags 'some-previous-tag latest latest.2 latest-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag latest latest.2 latest-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/eks-distro-minimal-base:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/eks-distro-minimal-base:$tag; done
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring eks-distro-minimal-base-builder tags 'some-previous-tag latest latest.2 latest-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag latest latest.2 latest-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/eks-distro-minimal-base-builder:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/eks-distro-minimal-base-builder:$tag; done
tag from file for eks-distro-minimal-base-nonroot: some-previous-tag
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring eks-distro-minimal-base-nonroot tags 'some-previous-tag latest latest.2 latest-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag latest latest.2 latest-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/eks-distro-minimal-base-nonroot:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/eks-distro-minimal-base-nonroot:$tag; done
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring eks-distro-minimal-base-nonroot-builder tags 'some-previous-tag latest latest.2 latest-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag latest latest.2 latest-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/eks-distro-minimal-base-nonroot-builder:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/eks-distro-minimal-base-nonroot-builder:$tag; done
tag from file for eks-distro-minimal-base-glibc: some-previous-tag
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring eks-distro-minimal-base-glibc tags 'some-previous-tag latest latest.2 latest-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag latest latest.2 latest-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/eks-distro-minimal-base-glibc:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/eks-distro-minimal-base-glibc:$tag; done
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring eks-distro-minimal-base-glibc-builder tags 'some-previous-tag latest latest.2 latest-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag latest latest.2 latest-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/eks-distro-minimal-base-glibc-builder:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/eks-distro-minimal-base-glibc-builder:$tag; done
tag from file for eks-distro-minimal-base-iptables: some-previous-tag
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring eks-distro-minimal-base-iptables tags 'some-previous-tag latest latest.2 latest-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag latest latest.2 latest-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/eks-distro-minimal-base-iptables:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/eks-distro-minimal-base-iptables:$tag; done
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring eks-distro-minimal-base-iptables-builder tags 'some-previous-tag latest latest.2 latest-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag latest latest.2 latest-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/eks-distro-minimal-base-iptables-builder:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/eks-distro-minimal-base-iptables-builder:$tag; done
tag from file for eks-distro-minimal-base-nsenter: some-previous-tag
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring eks-distro-minimal-base-nsenter tags 'some-previous-tag latest latest.2 latest-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag latest latest.2 latest-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/eks-distro-minimal-base-nsenter:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/eks-distro-minimal-base-nsenter:$tag; done
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring eks-distro-minimal-base-nsenter-builder tags 'some-previous-tag latest latest.2 latest-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag latest latest.2 latest-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/eks-distro-minimal-base-nsenter-builder:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/eks-distro-minimal-base-nsenter-builder:$tag; done
tag from file for eks-distro-minimal-base-docker-client: some-previous-tag
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring eks-distro-minimal-base-docker-client tags 'some-previous-tag latest latest.2 latest-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag latest latest.2 latest-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/eks-distro-minimal-base-docker-client:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/eks-distro-minimal-base-docker-client:$tag; done
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring eks-distro-minimal-base-docker-client-builder tags 'some-previous-tag latest latest.2 latest-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag latest latest.2 latest-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/eks-distro-minimal-base-docker-client-builder:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/eks-distro-minimal-base-docker-client-builder:$tag; done
tag from file for eks-distro-minimal-base-nginx: some-previous-tag
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring eks-distro-minimal-base-nginx tags 'some-previous-tag latest latest.2 latest-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag latest latest.2 latest-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/eks-distro-minimal-base-nginx:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/eks-distro-minimal-base-nginx:$tag; done
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring eks-distro-minimal-base-nginx-builder tags 'some-previous-tag latest latest.2 latest-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag latest latest.2 latest-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/eks-distro-minimal-base-nginx-builder:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/eks-distro-minimal-base-nginx-builder:$tag; done
tag from file for eks-distro-minimal-base-csi-ebs: some-previous-tag
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring eks-distro-minimal-base-csi-ebs tags 'some-previous-tag latest latest.2 latest-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag latest latest.2 latest-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/eks-distro-minimal-base-csi-ebs:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/eks-distro-minimal-base-csi-ebs:$tag; done
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring eks-distro-minimal-base-csi-ebs-builder tags 'some-previous-tag latest latest.2 latest-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag latest latest.2 latest-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/eks-distro-minimal-base-csi-ebs-builder:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/eks-distro-minimal-base-csi-ebs-builder:$tag; done
tag from file for eks-distro-minimal-base-csi: some-previous-tag
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring eks-distro-minimal-base-csi tags 'some-previous-tag latest latest.2 latest-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag latest latest.2 latest-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/eks-distro-minimal-base-csi:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/eks-distro-minimal-base-csi:$tag; done
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring eks-distro-minimal-base-csi-builder tags 'some-previous-tag latest latest.2 latest-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag latest latest.2 latest-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/eks-distro-minimal-base-csi-builder:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/eks-distro-minimal-base-csi-builder:$tag; done
tag from file for eks-distro-minimal-base-kind: some-previous-tag
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring eks-distro-minimal-base-kind tags 'some-previous-tag latest latest.2 latest-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag latest latest.2 latest-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/eks-distro-minimal-base-kind:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/eks-distro-minimal-base-kind:$tag; done
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring eks-distro-minimal-base-kind-builder tags 'some-previous-tag latest latest.2 latest-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag latest latest.2 latest-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/eks-distro-minimal-base-kind-builder:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/eks-distro-minimal-base-kind-builder:$tag; done
tag from file for eks-distro-minimal-base-haproxy: some-previous-tag
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring eks-distro-minimal-base-haproxy tags 'some-previous-tag latest latest.2 latest-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag latest latest.2 latest-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/eks-distro-minimal-base-haproxy:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/eks-distro-minimal-base-haproxy:$tag; done
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring eks-distro-minimal-base-haproxy-builder tags 'some-previous-tag latest latest.2 latest-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag latest latest.2 latest-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/eks-distro-minimal-base-haproxy-builder:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/eks-distro-minimal-base-haproxy-builder:$tag; done
tag from file for eks-distro-minimal-base-git: some-previous-tag
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring eks-distro-minimal-base-git tags 'some-previous-tag latest latest.2 latest-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag latest latest.2 latest-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/eks-distro-minimal-base-git:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/eks-distro-minimal-base-git:$tag; done
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring eks-distro-minimal-base-git-builder tags 'some-previous-tag latest latest.2 latest-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag latest latest.2 latest-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/eks-distro-minimal-base-git-builder:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/eks-distro-minimal-base-git-builder:$tag; done
tag from file for eks-distro-minimal-base-compiler-base: some-previous-tag
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring compiler-base tags 'some-previous-tag latest latest.2 latest-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag latest latest.2 latest-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/compiler-base:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/compiler-base:$tag; done
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring compiler-base-builder tags 'some-previous-tag latest latest.2 latest-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag latest latest.2 latest-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/compiler-base-builder:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/compiler-base-builder:$tag; done
tag from file for eks-distro-minimal-base-compiler-yum: some-previous-tag
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring compiler-base tags 'some-previous-tag yum-latest yum-latest.2 yum-latest-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag yum-latest yum-latest.2 yum-latest-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/compiler-base:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/compiler-base:$tag; done
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring compiler-base-builder tags 'some-previous-tag yum-latest yum-latest.2 yum-latest-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag yum-latest yum-latest.2 yum-latest-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/compiler-base-builder:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/compiler-base-builder:$tag; done
tag from file for eks-distro-minimal-base-compiler-gcc: some-previous-tag
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring compiler-base tags 'some-previous-tag gcc-latest gcc-latest.2 gcc-latest-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag gcc-latest gcc-latest.2 gcc-latest-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/compiler-base:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/compiler-base:$tag; done
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring compiler-base-builder tags 'some-previous-tag gcc-latest gcc-latest.2 gcc-latest-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag gcc-latest gcc-latest.2 gcc-latest-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/compiler-base-builder:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/compiler-base-builder:$tag; done
tag from file for eks-distro-minimal-base-python-3.9: some-previous-tag
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring eks-distro-minimal-base-python tags 'some-previous-tag 3.9-latest 3.9-latest.2 3.9-latest-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag 3.9-latest 3.9-latest.2 3.9-latest-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/eks-distro-minimal-base-python:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/eks-distro-minimal-base-python:$tag; done
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring eks-distro-minimal-base-python-builder tags 'some-previous-tag 3.9-latest 3.9-latest.2 3.9-latest-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag 3.9-latest 3.9-latest.2 3.9-latest-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/eks-distro-minimal-base-python-builder:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/eks-distro-minimal-base-python-builder:$tag; done
tag from file for eks-distro-minimal-base-golang-compiler-1.15-base: some-previous-tag
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring golang tags 'some-previous-tag 1.15 1.15.15-mock 1.15-al2 1.15.15-mock-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag 1.15 1.15.15-mock 1.15-al2 1.15.15-mock-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/golang:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/golang:$tag; done
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring golang-builder tags 'some-previous-tag 1.15 1.15.15-mock 1.15-al2 1.15.15-mock-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag 1.15 1.15.15-mock 1.15-al2 1.15.15-mock-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/golang-builder:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/golang-builder:$tag; done
tag from file for eks-distro-minimal-base-golang-compiler-1.15-yum: some-previous-tag
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring golang tags 'some-previous-tag 1.15-yum 1.15.15-mock-yum 1.15-yum-al2 1.15.15-mock-yum-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag 1.15-yum 1.15.15-mock-yum 1.15-yum-al2 1.15.15-mock-yum-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/golang:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/golang:$tag; done
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring golang-builder tags 'some-previous-tag 1.15-yum 1.15.15-mock-yum 1.15-yum-al2 1.15.15-mock-yum-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag 1.15-yum 1.15.15-mock-yum 1.15-yum-al2 1.15.15-mock-yum-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/golang-builder:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/golang-builder:$tag; done
tag from file for eks-distro-minimal-base-golang-compiler-1.15-gcc: some-previous-tag
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring golang tags 'some-previous-tag 1.15-gcc 1.15.15-mock-gcc 1.15-gcc-al2 1.15.15-mock-gcc-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag 1.15-gcc 1.15.15-mock-gcc 1.15-gcc-al2 1.15.15-mock-gcc-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/golang:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/golang:$tag; done
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring golang-builder tags 'some-previous-tag 1.15-gcc 1.15.15-mock-gcc 1.15-gcc-al2 1.15.15-mock-gcc-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag 1.15-gcc 1.15.15-mock-gcc 1.15-gcc-al2 1.15.15-mock-gcc-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/golang-builder:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/golang-builder:$tag; done
tag from file for eks-distro-minimal-base-golang-compiler-1.16-base: some-previous-tag
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring golang tags 'some-previous-tag 1.16 1.16.15-mock 1.16-al2 1.16.15-mock-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag 1.16 1.16.15-mock 1.16-al2 1.16.15-mock-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/golang:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/golang:$tag; done
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring golang-builder tags 'some-previous-tag 1.16 1.16.15-mock 1.16-al2 1.16.15-mock-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag 1.16 1.16.15-mock 1.16-al2 1.16.15-mock-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/golang-builder:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/golang-builder:$tag; done
tag from file for eks-distro-minimal-base-golang-compiler-1.16-yum: some-previous-tag
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring golang tags 'some-previous-tag 1.16-yum 1.16.15-mock-yum 1.16-yum-al2 1.16.15-mock-yum-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag 1.16-yum 1.16.15-mock-yum 1.16-yum-al2 1.16.15-mock-yum-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/golang:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/golang:$tag; done
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring golang-builder tags 'some-previous-tag 1.16-yum 1.16.15-mock-yum 1.16-yum-al2 1.16.15-mock-yum-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag 1.16-yum 1.16.15-mock-yum 1.16-yum-al2 1.16.15-mock-yum-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/golang-builder:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/golang-builder:$tag; done
tag from file for eks-distro-minimal-base-golang-compiler-1.16-gcc: some-previous-tag
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring golang tags 'some-previous-tag 1.16-gcc 1.16.15-mock-gcc 1.16-gcc-al2 1.16.15-mock-gcc-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag 1.16-gcc 1.16.15-mock-gcc 1.16-gcc-al2 1.16.15-mock-gcc-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/golang:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/golang:$tag; done
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring golang-builder tags 'some-previous-tag 1.16-gcc 1.16.15-mock-gcc 1.16-gcc-al2 1.16.15-mock-gcc-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag 1.16-gcc 1.16.15-mock-gcc 1.16-gcc-al2 1.16.15-mock-gcc-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/golang-builder:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/golang-builder:$tag; done
tag from file for eks-distro-minimal-base-golang-compiler-1.17-base: some-previous-tag
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring golang tags 'some-previous-tag 1.17 1.17.13-mock 1.17-al2 1.17.13-mock-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag 1.17 1.17.13-mock 1.17-al2 1.17.13-mock-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/golang:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/golang:$tag; done
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring golang-builder tags 'some-previous-tag 1.17 1.17.13-mock 1.17-al2 1.17.13-mock-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag 1.17 1.17.13-mock 1.17-al2 1.17.13-mock-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/golang-builder:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/golang-builder:$tag; done
tag from file for eks-distro-minimal-base-golang-compiler-1.17-yum: some-previous-tag
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring golang tags 'some-previous-tag 1.17-yum 1.17.13-mock-yum 1.17-yum-al2 1.17.13-mock-yum-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag 1.17-yum 1.17.13-mock-yum 1.17-yum-al2 1.17.13-mock-yum-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/golang:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/golang:$tag; done
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring golang-builder tags 'some-previous-tag 1.17-yum 1.17.13-mock-yum 1.17-yum-al2 1.17.13-mock-yum-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag 1.17-yum 1.17.13-mock-yum 1.17-yum-al2 1.17.13-mock-yum-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/golang-builder:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/golang-builder:$tag; done
tag from file for eks-distro-minimal-base-golang-compiler-1.17-gcc: some-previous-tag
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring golang tags 'some-previous-tag 1.17-gcc 1.17.13-mock-gcc 1.17-gcc-al2 1.17.13-mock-gcc-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag 1.17-gcc 1.17.13-mock-gcc 1.17-gcc-al2 1.17.13-mock-gcc-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/golang:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/golang:$tag; done
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring golang-builder tags 'some-previous-tag 1.17-gcc 1.17.13-mock-gcc 1.17-gcc-al2 1.17.13-mock-gcc-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag 1.17-gcc 1.17.13-mock-gcc 1.17-gcc-al2 1.17.13-mock-gcc-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/golang-builder:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/golang-builder:$tag; done
tag from file for eks-distro-minimal-base-golang-compiler-1.18-base: some-previous-tag
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring golang tags 'some-previous-tag 1.18 1.18.9-mock 1.18-al2 1.18.9-mock-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag 1.18 1.18.9-mock 1.18-al2 1.18.9-mock-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/golang:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/golang:$tag; done
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring golang-builder tags 'some-previous-tag 1.18 1.18.9-mock 1.18-al2 1.18.9-mock-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag 1.18 1.18.9-mock 1.18-al2 1.18.9-mock-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/golang-builder:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/golang-builder:$tag; done
tag from file for eks-distro-minimal-base-golang-compiler-1.18-yum: some-previous-tag
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring golang tags 'some-previous-tag 1.18-yum 1.18.9-mock-yum 1.18-yum-al2 1.18.9-mock-yum-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag 1.18-yum 1.18.9-mock-yum 1.18-yum-al2 1.18.9-mock-yum-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/golang:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/golang:$tag; done
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring golang-builder tags 'some-previous-tag 1.18-yum 1.18.9-mock-yum 1.18-yum-al2 1.18.9-mock-yum-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag 1.18-yum 1.18.9-mock-yum 1.18-yum-al2 1.18.9-mock-yum-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/golang-builder:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/golang-builder:$tag; done
tag from file for eks-distro-minimal-base-golang-compiler-1.18-gcc: some-previous-tag
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring golang tags 'some-previous-tag 1.18-gcc 1.18.9-mock-gcc 1.18-gcc-al2 1.18.9-mock-gcc-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag 1.18-gcc 1.18.9-mock-gcc 1.18-gcc-al2 1.18.9-mock-gcc-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/golang:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/golang:$tag; done
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring golang-builder tags 'some-previous-tag 1.18-gcc 1.18.9-mock-gcc 1.18-gcc-al2 1.18.9-mock-gcc-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag 1.18-gcc 1.18.9-mock-gcc 1.18-gcc-al2 1.18.9-mock-gcc-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/golang-builder:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/golang-builder:$tag; done
tag from file for eks-distro-minimal-base-golang-compiler-1.19-base: some-previous-tag
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring golang tags 'some-previous-tag 1.19 1.19.4-mock 1.19-al2 1.19.4-mock-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag 1.19 1.19.4-mock 1.19-al2 1.19.4-mock-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/golang:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/golang:$tag; done
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring golang-builder tags 'some-previous-tag 1.19 1.19.4-mock 1.19-al2 1.19.4-mock-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag 1.19 1.19.4-mock 1.19-al2 1.19.4-mock-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/golang-builder:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/golang-builder:$tag; done
tag from file for eks-distro-minimal-base-golang-compiler-1.19-yum: some-previous-tag
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring golang tags 'some-previous-tag 1.19-yum 1.19.4-mock-yum 1.19-yum-al2 1.19.4-mock-yum-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag 1.19-yum 1.19.4-mock-yum 1.19-yum-al2 1.19.4-mock-yum-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/golang:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/golang:$tag; done
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring golang-builder tags 'some-previous-tag 1.19-yum 1.19.4-mock-yum 1.19-yum-al2 1.19.4-mock-yum-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag 1.19-yum 1.19.4-mock-yum 1.19-yum-al2 1.19.4-mock-yum-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/golang-builder:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/golang-builder:$tag; done
tag from file for eks-distro-minimal-base-golang-compiler-1.19-gcc: some-previous-tag
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring golang tags 'some-previous-tag 1.19-gcc 1.19.4-mock-gcc 1.19-gcc-al2 1.19.4-mock-gcc-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag 1.19-gcc 1.19.4-mock-gcc 1.19-gcc-al2 1.19.4-mock-gcc-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/golang:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/golang:$tag; done
if [ "some-previous-tag" = "null" ]; then exit; fi && printf "Mirroring golang-builder tags 'some-previous-tag 1.19-gcc 1.19.4-mock-gcc 1.19-gcc-al2 1.19.4-mock-gcc-al2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in some-previous-tag 1.19-gcc 1.19.4-mock-gcc 1.19-gcc-al2 1.19.4-mock-gcc-al2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/golang-builder:some-previous-tag docker://public.ecr.aws/eks-distro-build-tooling/golang-builder:$tag; done
if [ "1809-2022-12-19-1671485888.2" = "null" ]; then exit; fi && printf "Mirroring eks-distro-windows-base tags '1809-2022-12-19-1671485888.2 1809' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in 1809-2022-12-19-1671485888.2 1809; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/eks-distro-windows-base:1809-2022-12-19-1671485888.2 docker://public.ecr.aws/eks-distro-build-tooling/eks-distro-windows-base:$tag; done
if [ "20H2-2022-12-19-1671485888.2" = "null" ]; then exit; fi && printf "Mirroring eks-distro-windows-base tags '20H2-2022-12-19-1671485888.2 20H2' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in 20H2-2022-12-19-1671485888.2 20H2; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/eks-distro-windows-base:20H2-2022-12-19-1671485888.2 docker://public.ecr.aws/eks-distro-build-tooling/eks-distro-windows-base:$tag; done
if [ "ltsc2022-2022-12-19-1671485888.2" = "null" ]; then exit; fi && printf "Mirroring eks-distro-windows-base tags 'ltsc2022-2022-12-19-1671485888.2 ltsc2022' from localhost:5000 to public.ecr.aws/eks-distro-build-tooling\n\n" && set -x && for tag in ltsc2022-2022-12-19-1671485888.2 ltsc2022; do skopeo copy --all --retry-times 15 --preserve-digests docker://localhost:5000/eks-distro-windows-base:ltsc2022-2022-12-19-1671485888.2 docker://public.ecr.aws/eks-distro-build-tooling/eks-distro-windows-base:$tag; done
